---
// Props
const { orientation = 'horizontal', className = '' } = Astro.props;

// Determine orientation class
const orientationClass =
  orientation === 'vertical' ? 'w-[2px] h-full' : 'h-[2px] w-full';
---
<div class={`bg-border shrink-0 ${orientationClass} ${className}`} role="separator" />

